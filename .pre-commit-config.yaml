repos:
  - repo: local
    hooks:
      - id: semgrep
        name: Semgrep Security Scan
        entry: uv run semgrep --error --config .semgrep/rules.yaml
        language: system
        files: \.py$